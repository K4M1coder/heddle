target/
__pycache__/
.venv/
node_modules/
dist/
*.log
.env

# personal prototype (not part of the product; kept locally, untracked)
protoharness.py

target/
Cargo.lock
target/

# Tauri's generated capability/permission schemas: a build product of
# `tauri-build`, regenerated on every build, never hand-edited.
ui/src-tauri/gen/

# Archon keeps per-run telemetry outside the repo, but an older layout wrote
# these here. They are local-only and must never be committed.
.archon/artifacts/
.archon/logs/
.archon/state/

# Playwright MCP's page snapshots/console logs, written to the cwd it was
# invoked from -- local-only, never part of the product.
.playwright-mcp/
