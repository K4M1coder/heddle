//! Finding the profiles Skein made, and taking them back.

use crate::{Grant, GrantKind, GrantState, GrantedDir, Pruned};

pub(crate) fn grants() -> Result<Vec<Grant>, String> {
    todo!("T4")
}

pub(crate) fn prune(_profile: &str) -> Result<Pruned, String> {
    todo!("T5")
}
